[package]
name = "dhee-accel"
version = "0.1.0"
edition = "2021"
description = "Rust acceleration for Dhee memory layer"
license = "MIT"

[lib]
name = "dhee_accel"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module"] }
rayon = "1.10"
